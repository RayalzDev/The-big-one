const BASE_URL = "http://localhost:3001/";
const USUARIO = `${BASE_URL}usuario/`;
const EDITUSUARIO = `${BASE_URL}usuario/<ID>`;
const LOGIN = `${BASE_URL}login/`;
const EMPRESAS = `${BASE_URL}empresas/`;
const UNAEMPRESA = `${BASE_URL}empresas/<NAME>`;
const UNUSUARIO = `${BASE_URL}usuarios/<ID>`;

export { BASE_URL, USUARIO, LOGIN, EMPRESAS, UNUSUARIO, UNAEMPRESA, EDITUSUARIO };
